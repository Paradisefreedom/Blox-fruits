local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false
Library.ShowToggleFrameInKeybinds = true

local Window = Library:CreateWindow({

	Title = "Hypex",
	Footer = "version: 1.0.0",
	Icon = 95816097006870,
	NotifySide = "Right",
	ShowCustomCursor = true,
})

local Tabs = {
	-- Creates a new tab titled Main
	Main = Window:AddTab("Main", "user"),
	["UI Settings"] = Window:AddTab("Config", "Gear"),
}

local UISettingsTab = Tabs["UI Settings"]

local LeftGroupBox = Tabs.Main:AddGroupbox({
	Side = "Left", --// (Case-insensitive),
	Name = "Auto Farm",
	Description = "Auto",
	IconName = "boxes",
	-- Collapsed = false,
	-- DisableCollapsing = false,
})

local RightGroupBox = Tabs.Main:AddGroupbox({
	Side = "Right", --// (Case-insensitive),
	Name = "Magnet",
	Description = "Auto Farms magnet event",
	IconName = "boxes",
	-- Collapsed = false,
	-- DisableCollapsing = false,
})

local TabBox = Tabs.Main:AddLeftTabbox()

local Tab1 = TabBox:AddTab("Hover")
local Tab2 = TabBox:AddTab("Teleport")

local Slider = Tab2:AddSlider("MySlider", {
    Text = "Delay",
    Default = 10,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Compact = false,
    Callback = function(Value)
        print("Slider changed to:", Value)
    end
})

local Slider = Tab1:AddSlider("MySlider", {
    Text = "Hover Speed",
    Default = 30,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Compact = false,
    Callback = function(Value)
        print("Slider changed to:", Value)
    end
})

-- Groupbox:AddToggle
-- Arguments: Index, Options
LeftGroupBox:AddToggle("MyToggle", {
	Text = "AutoFarm",
	Tooltip = "Auto Farm Levels", -- Information shown when you hover over the toggle
	DisabledTooltip = "I am disabled!", -- Information shown when you hover over the toggle while it's disabled

	Default = true, -- Default value (true / false)
	Disabled = false, -- Will disable the toggle (true / false)
	Visible = true, -- Will make the toggle invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)

	Callback = function(Value)
		print("[cb] MyToggle changed to:", Value)
	end,
})

--[[
	Groupbox:AddButton
	Arguments: {
		Text = string,
		Func = function,
		DoubleClick = boolean
		Tooltip = string,
	}

	You can call :AddButton on a button to add a SubButton!
]]

local MyButton = RightGroupBox:AddButton({
	Text = "Open Magnet gacha Menu",
	Func = function()
		print("You clicked")
	end,
	DoubleClick = false,

	Tooltip = "Takes you to Gacha menu (for magnet)",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})


local Tabs = {
	-- MISC TAB
	Misc = Window:AddTab("Misc", "Gear"),
}

local LeftGroupBox = Tabs.Misc:AddGroupbox({
	Side = "Left", --// (Case-insensitive),
	Name = "Settings",
	Description = "Codes, FPS, ETC",
	IconName = "boxes",
	-- Collapsed = false,
	-- DisableCollapsing = false,
})

local MyButton = LeftGroupBox:AddButton({
	Text = "Auto Redeem Codes",
	Func = function()
		print("Redeemed all codes")
	end,
	DoubleClick = false,

	Tooltip = "Redeemed all codes",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = LeftGroupBox:AddButton({
	Text = "FPS BOOST",
	Func = function()
		print("FPS BOOSTED")
	end,
	DoubleClick = false,

	Tooltip = "FPS BOOST",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = LeftGroupBox:AddButton({
	Text = "White Screen",
	Func = function()
		print("Screen whitened")
	end,
	DoubleClick = false,

	Tooltip = "Makes your screen white",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = LeftGroupBox:AddButton({
	Text = "Black Screen",
	Func = function()
		print("Screen Blackened")
	end,
	DoubleClick = false,

	Tooltip = "Makes your screen Black",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local RightGroupBox = Tabs.Misc:AddGroupbox({
	Side = "Right", --// (Case-insensitive),
	Name = "Servers",
	Description = "Codes, FPS, ETC",
	IconName = "boxes",
	-- Collapsed = false,
	-- DisableCollapsing = false,
})

local MyButton = RightGroupBox:AddButton({
	Text = "Server Hop",
	Func = function()
		print("Hopping servers")
	end,
	DoubleClick = false,

	Tooltip = "Hopping server",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = RightGroupBox:AddButton({
	Text = "Full Moon",
	Func = function()
		print("Looking for server")
	end,
	DoubleClick = false,

	Tooltip = "Full Moon server",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = RightGroupBox:AddButton({
	Text = "Near Full Moon",
	Func = function()
		print("Looking for server")
	end,
	DoubleClick = false,

	Tooltip = "Near Moon server",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local MyButton = RightGroupBox:AddButton({
	Text = "Mirage",
	Func = function()
		print("Looking for server")
	end,
	DoubleClick = false,

	Tooltip = "Mirage server",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local Tabs = {
	-- SEA EVENTS TAB
	Sea = Window:AddTab("Sea Events", "Sea"),
}

local LeftGroupBox = Tabs.Misc:AddGroupbox({
	Side = "Left", --// (Case-insensitive),
	Name = "Kitsune",
	Description = "Find, Go, Redeem",
	IconName = "boxes",
	-- Collapsed = false,
	-- DisableCollapsing = false,
})

local MyButton = LeftGroupBox:AddButton({
	Text = "Auto Redeem Azure",
	Func = function()
		print("Redeemed all Azure")
	end,
	DoubleClick = false,

	Tooltip = "Redeem all azure",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local Mybutton = LeftGroupBox:AddButton({
    Text = "Auto Find"
    Func = function()
		print("Looking for Kitsune Shrine")
	end,
	DoubleClick = false,

	Tooltip = "Look for Kitune Shrine",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})

local Mybutton = LeftGroupBox:AddButton({
    Text = "Auto Go to Shrine"
    Func = function()
		print("Going for Kitsune Shrine")
	end,
	DoubleClick = false,

	Tooltip = "Look for Kitune Shrine",
	DisabledTooltip = "I am disabled!",

	Disabled = false, -- Will disable the button (true / false)
	Visible = true, -- Will make the button invisible (true / false)
	Risky = false, -- Makes the text red (the color can be changed using Library.Scheme.Red) (Default value = false)
})
